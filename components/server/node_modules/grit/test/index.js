(function() {
  var collectionBatch, modelBatch, suite, vows;

  vows = require('vows');

  modelBatch = require('./model');

  collectionBatch = require('./collection');

  suite = vows.describe('Grit');

  modelBatch(suite);

  collectionBatch(suite);

  suite.run();

}).call(this);
